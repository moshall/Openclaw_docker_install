# 2026-03-10 Follow-up Plan (Based on codex/task-2026-03-09)

## Baseline
- Branch: `codex/task-2026-03-09`
- Baseline commit: `73e49cb`
- Goal: continue iteration in small tasks to avoid long-chat context pollution.

## Completed in Baseline
1. Added runtime env persistence for:
   - `/root/.pip`
   - `/root/.cargo/config`
   - `/root/.cargo/config.toml`
2. Covered migration copy + runtime mounts + compose export + auto-detect.
3. Full regression passed before baseline commit.

## Next Candidate Work (User-confirmed Direction)
1. Decouple software container path from `/root/.openclaw/software` to a standalone root (e.g. `/root/.openclaw-software`).
2. Keep backward compatibility symlink:
   - `/root/.openclaw/software -> /root/.openclaw-software`
3. Unify software install/discover/rebuild paths behind one constant.
4. Add migration for legacy data path on first upgrade/rebuild.
5. Keep strict TDD: RED -> GREEN -> full regression -> commit per task.

## TDD Workflow Per Task
1. Add/adjust test assertions first.
2. Run target test to confirm RED.
3. Implement minimal code to GREEN.
4. Run full regression:
   - `bash tests/openclawctl_test.sh`
   - `bash tests/quick_start_test.sh`
   - `bash tests/installer_v07_smoke_test.sh`
   - `bash tests/installer_v07_1panel_test.sh`
   - `bash tests/installer_v07_compose_test.sh`
   - `go test ./...`
5. Commit with focused message.

## Notes
- Do not merge into `main` directly in this thread.
- Keep branch incremental; push branch only when requested.
